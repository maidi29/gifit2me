{
  "name": "gifit2me",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "(cd server && npm install) && (cd client && npm install) && (cd client && npm start) && (cd server && npm start)"
  }
}
